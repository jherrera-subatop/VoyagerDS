import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Tokens are compiled in predev/prebuild scripts — NOT here.
  // See terrazzo.config.mjs for the token pipeline.
};

export default nextConfig;
